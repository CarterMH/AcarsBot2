version: '3.8'

services:
  acars-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: acars-discord-bot
    restart: unless-stopped
    ports:
      - "3000:3000"
    networks:
      - acars-network

networks:
  acars-network:
    driver: bridge
