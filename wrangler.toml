name = "acars-bot-api"
main = "worker.js"
compatibility_date = "2026-01-07"
compatibility_flags = ["nodejs_compat"]

# Environment variables should be set in Cloudflare dashboard
# Settings > Variables and Secrets > Environment Variables
# Required vars: DISCORD_TOKEN, ANNOUNCEMENT_CHANNEL_ID, SUPABASE_URL, SUPABASE_ANON_KEY, ADMIN_PASSWORD